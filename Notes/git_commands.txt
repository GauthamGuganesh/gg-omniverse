GIT COMMANDS:

—Cloning:
	cd <path>
	git clone <url>

Whenever push, pull or commit, be inside <path>

—Updating:
	git pull origin <branch name> 

Default branch - master

—Commiting:
	git commit -a -m “<Comment>”
	git push origin <branch name>

— Adding files:
git add <filepath> - for adding a new file in the local repo.
After adding do commit.
